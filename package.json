{
  "name": "winccoa-extensions-workspace",
  "version": "1.0.0",
  "description": "WinCC OA Extensions - Monorepo workspace",
  "author": "ETM professional control",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "winccoa-extensions/data/extensions",
    "winccoa-extensions/javascript/winccoa-extensions"
  ],
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm run build --workspace=winccoa-extensions/javascript/winccoa-extensions",
    "build:frontend": "npm run build --workspace=winccoa-extensions/data/extensions",
    "install:all": "npm install",
    "lint": "npm run lint --workspace=winccoa-extensions/javascript/winccoa-extensions",
    "format": "npm run format --workspace=winccoa-extensions/javascript/winccoa-extensions && npm run format --workspace=winccoa-extensions/data/extensions",
    "format:backend": "npm run format --workspace=winccoa-extensions/javascript/winccoa-extensions",
    "format:frontend": "npm run format --workspace=winccoa-extensions/data/extensions",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}